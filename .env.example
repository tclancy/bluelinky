# Hyundai Bluelink API Credentials
# Copy this file to .env and fill in your actual credentials

# Your Bluelink account email
BLUELINK_USERNAME=your_email@example.com

# Your Bluelink account password
BLUELINK_PASSWORD=your_password

# Your 4-digit PIN for remote operations
BLUELINK_PIN=1234

# Vehicle brand (hyundai or kia)
BLUELINK_BRAND=hyundai

# Region (US, CA, EU, AU, CN)
BLUELINK_REGION=US

# Optional: Vehicle VIN (not currently used but may be needed in future)
BLUELINK_VIN=your_vehicle_vin

# Optional: Alert backend (console, ntfy, sns, tmobile-email, or email)
# ntfy is the recommended backend for homelab deployments
ALERT_BACKEND=ntfy

# Optional: Test mode - force an alert for testing (low | critical | tpms)
# TEST_ALERT=low

# ==================== TPMS + Fuel History Options ====================

# Home location for outdoor temperature correlation (Open-Meteo, free, no API key)
# Default: Dover, NH
HOME_LAT=43.2419
HOME_LON=-70.9164458

# Miles of range increase to classify as a fill-up event (default: 20)
# FUEL_FILLUP_THRESHOLD=20

# Override path for the SQLite database (default: state/vehicle-monitor.db)
# VEHICLE_DB_PATH=/app/state/vehicle-monitor.db

# ==================== Alert Backend Options ====================

# --- ntfy (RECOMMENDED for homelab) ---
# Uses your self-hosted ntfy instance for push notifications.
# ALERT_BACKEND=ntfy
NTFY_URL=https://notifications.example.com/fuelbot    # Full topic URL
NTFY_USERNAME=your_ntfy_username
NTFY_PASSWORD=your_ntfy_password
#
# Credentials are managed via Ansible Vault (homelab repo). See README.md.

# --- T-Mobile Email-to-SMS (FREE!) ---
# Uses T-Mobile's email gateway to send free text messages
# Requires Gmail (or other SMTP) account for sending
# ALERT_BACKEND=tmobile-email
# TMOBILE_PHONES=5551234567,5559876543        # 10-digit phone numbers (no +1 or dashes)
# EMAIL_FROM=youremail@gmail.com              # Your Gmail address
# EMAIL_SMTP_HOST=smtp.gmail.com              # Gmail SMTP server
# EMAIL_SMTP_PORT=587                         # Gmail SMTP port (587 for TLS, 465 for SSL)
# EMAIL_SMTP_USER=youremail@gmail.com         # Your Gmail address
# EMAIL_SMTP_PASSWORD=your_app_password       # Gmail App Password (NOT your regular password!)
#
# To create a Gmail App Password:
# 1. Go to https://myaccount.google.com/apppasswords
# 2. Select "Mail" and "Other (Custom name)"
# 3. Name it "Fuel Monitor" and click Generate
# 4. Copy the 16-character password (no spaces)

# --- AWS SNS (RECOMMENDED! Easiest and most flexible) ---
# Works with SNS Topics - no sandbox mode or origination entity issues!
# Free tier: 1000 notifications/month for first 12 months
# After free tier: ~$0.50 per million notifications
# ALERT_BACKEND=sns
# AWS_SNS_TOPIC_ARN=arn:aws:sns:us-east-1:123456789:YourTopicName  # SNS Topic ARN
# AWS_REGION=us-east-1                        # AWS region (default: us-east-1)
# AWS_ACCESS_KEY_ID=your_access_key_id        # IAM user access key (optional if using IAM role)
# AWS_SECRET_ACCESS_KEY=your_secret_key       # IAM user secret key (optional if using IAM role)
#
# To set up AWS SNS with Topics (easier than direct SMS):
# 1. Create AWS account at https://aws.amazon.com
# 2. Go to SNS console and create a new topic
# 3. Create a subscription to the topic (email, SMS, etc.)
# 4. Create IAM user with SNS:Publish permission
# 5. Copy the Topic ARN to AWS_SNS_TOPIC_ARN above
#
# Alternative: Direct SMS (requires sandbox exit)
# AWS_SNS_PHONES=+15551234567,+15559876543    # Phone numbers with country code (comma-separated)
# Note: Direct SMS requires exiting SNS sandbox mode which can take 24-48 hours
